<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="io.slib.call">

    <application>

        <service
            android:name="slib.call.android.CallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            >
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>
<!--
        <service
            android:name="slib.call.android.ListenerService"
            android:enabled="true"
            >
            <intent-filter android:priority="1000">
                <action android:name="slib.call.android.ListenerService" />
            </intent-filter>
        </service>
-->
    </application>

</manifest>
